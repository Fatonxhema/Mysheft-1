/*public class SearchLibrin {

private static void SortOddEven(String[] input, String userinput)
        {
        var sort = false;
        while (!sort) {
            sort = true;
            for (var i = 1; i < userinput.length() - 1; i += 2) {
                if (input[i] <= input.length(i) + 1 continue;
                var temp = input[i];
                input[i] = input[i + 1];
                input[i + 1] = temp;
                sort = false;
            }
            for (var i = 0; i < userinput.length() - 1; i += 2) {
                if (input[i]<= input.length(i)+ 1) continue;
                var temp = input[i];
                input[i] = input[i + 1];
                input[i + 1] = temp;
                sort = false;

            }
        }
        }

}*/